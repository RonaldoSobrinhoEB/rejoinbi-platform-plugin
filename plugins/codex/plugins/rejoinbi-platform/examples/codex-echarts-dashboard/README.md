# Codex ECharts Test Dashboard

Small static dashboard used to smoke-test the Rejoin BI plugin upload flow.

Recommended upload command:

```powershell
python "$HOME\plugins\rejoinbi-platform\scripts\rejoinbi.py" upload-folder-select --workspace codex-plugin-test-20260522 --path "$HOME\plugins\rejoinbi-platform\examples\codex-echarts-dashboard" --startup-mode static --auto-start
```
