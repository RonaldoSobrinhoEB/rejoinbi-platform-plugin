# Rejoin BI Executive Signal

Polished static ECharts dashboard used to smoke-test the Rejoin BI plugin upload flow, visual rendering, responsive layout, and chart behavior.

Recommended upload command:

```powershell
python "$HOME\plugins\rejoinbi\scripts\rejoinbi.py" upload-folder-select --workspace codex-plugin-test-20260522 --path "$HOME\plugins\rejoinbi\examples\codex-echarts-dashboard" --startup-mode static --auto-start
```
